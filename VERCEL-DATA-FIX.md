# إصلاح مشكلة عدم ظهور البيانات على Vercel

## المشكلة
البيانات موجودة محلياً (21 موظف، 898 حضور، 61 راتب) لكن لا تظهر على Vercel.

## السبب
Vercel يستخدم Environment Variables مختلفة أو قاعدة بيانات مختلفة عن المحلي.

## الحل

### الخطوة 1: التحقق من Environment Variables في Vercel

1. **اذهب إلى Vercel Dashboard:**
   - https://vercel.com/dashboard
   - اختر مشروع `svc-payroll`

2. **اذهب إلى Settings > Environment Variables**

3. **تحقق من وجود المتغيرات التالية:**
   ```
   NEXT_PUBLIC_SUPABASE_URL
   NEXT_PUBLIC_SUPABASE_ANON_KEY
   DATABASE_URL
   DIRECT_URL (اختياري)
   ```

4. **تأكد من أن القيم مطابقة لملف `.env.local` المحلي:**
   - يجب أن تكون نفس القيم بالضبط
   - لا تضع علامات اقتباس حول القيم في Vercel
   - تأكد من أن `DATABASE_URL` يشير لنفس قاعدة البيانات

### الخطوة 2: التحقق من قاعدة البيانات

**مهم جداً:** تأكد من أن `DATABASE_URL` في Vercel يشير لنفس قاعدة البيانات المحلية.

للتحقق:
1. افتح ملف `.env.local` المحلي
2. انسخ قيمة `DATABASE_URL`
3. تأكد من أن نفس القيمة موجودة في Vercel Environment Variables

### الخطوة 3: إعادة Deployment

بعد تحديث Environment Variables:

1. **اذهب إلى Deployments tab في Vercel**
2. **اضغط على "Redeploy"** للـ deployment الأخير
3. **أو ادفع commit جديد إلى GitHub** (سيتم deployment تلقائياً)

### الخطوة 4: التحقق من البيانات على Vercel

بعد إعادة Deployment:

1. افتح https://svc-payroll.vercel.app/dashboard
2. يجب أن تظهر جميع البيانات

## إذا استمرت المشكلة

### الحل البديل: استيراد البيانات مباشرة إلى قاعدة بيانات Vercel

إذا كان Vercel يستخدم قاعدة بيانات مختلفة:

1. **تحقق من قاعدة البيانات المستخدمة في Vercel:**
   - اذهب إلى Vercel Dashboard > Settings > Environment Variables
   - انسخ `DATABASE_URL`

2. **استخدم script الاستيراد مع DATABASE_URL من Vercel:**
   ```bash
   # مؤقتاً، غيّر DATABASE_URL في .env.local إلى قيمة Vercel
   # ثم شغّل:
   node scripts/import-local-data.js "مسار_ملف_النسخة_الاحتياطية.json"
   ```

3. **أو استخدم Prisma Studio مع DATABASE_URL من Vercel:**
   ```bash
   # غيّر DATABASE_URL في .env.local مؤقتاً
   npm run prisma:studio
   # ثم أضف البيانات يدوياً
   ```

## ملاحظات مهمة

- ✅ **البيانات موجودة محلياً:** 21 موظف، 898 حضور، 61 راتب
- ⚠️ **المشكلة:** Vercel لا يرى البيانات لأن Environment Variables مختلفة
- 💡 **الحل:** تأكد من أن Vercel يستخدم نفس `DATABASE_URL` المحلي

## التحقق السريع

شغّل هذا الأمر محلياً للتحقق من البيانات:
```bash
node scripts/check-database-data.js
```

يجب أن ترى:
```
📊 عدد الموظفين: 21
📅 عدد سجلات الحضور: 898
💰 عدد قسائم الرواتب: 61
```

إذا رأيت هذه الأرقام، البيانات موجودة محلياً والمشكلة في إعدادات Vercel.

