# رفع المشروع إلى GitHub - خطوات يدوية

## ⚠️ ملاحظة مهمة
إذا كان Git غير مثبت، يجب تثبيته أولاً من: https://git-scm.com/download/win

## 📝 الخطوات (نسخ ولصق)

افتح PowerShell في مجلد المشروع وانسخ الأوامر التالية واحداً تلو الآخر:

### 1. الانتقال إلى مجلد المشروع
```powershell
cd "C:\Users\HR\Videos\Payroll System"
```

### 2. تهيئة Git
```powershell
git init
```

### 3. إضافة Remote Repository
```powershell
git remote add origin https://github.com/hrsvciq-cm/SVC-Payroll.git
```

إذا ظهرت رسالة "remote origin already exists"، استخدم:
```powershell
git remote set-url origin https://github.com/hrsvciq-cm/SVC-Payroll.git
```

### 4. التحقق من Remote
```powershell
git remote -v
```

يجب أن يظهر:
```
origin  https://github.com/hrsvciq-cm/SVC-Payroll.git (fetch)
origin  https://github.com/hrsvciq-cm/SVC-Payroll.git (push)
```

### 5. إضافة جميع الملفات
```powershell
git add .
```

### 6. عمل Commit
```powershell
git commit -m "Initial commit: Payroll System with optimizations"
```

### 7. تعيين Branch الرئيسي
```powershell
git branch -M main
```

### 8. رفع المشروع
```powershell
git push -u origin main
```

## 🔐 عند طلب Authentication

### اسم المستخدم:
```
hrsvciq-cm
```

### كلمة المرور:
**استخدم Personal Access Token** (ليس كلمة المرور العادية)

#### كيفية إنشاء Token:
1. اذهب إلى: https://github.com/settings/tokens
2. انقر "Generate new token (classic)"
3. اختر الصلاحيات: ✅ `repo` (Full control)
4. انسخ الـ Token
5. استخدمه ككلمة مرور عند الطلب

## ✅ التحقق من النجاح

بعد الرفع، افتح:
https://github.com/hrsvciq-cm/SVC-Payroll

يجب أن ترى جميع الملفات.

## 🔧 حل المشاكل

### "git is not recognized"
**الحل**: تثبيت Git من https://git-scm.com/download/win

### "Authentication failed"
**الحل**: استخدام Personal Access Token

### "Permission denied"
**الحل**: 
1. تأكد من الصلاحيات على المستودع
2. استخدم Personal Access Token

### "Remote already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/hrsvciq-cm/SVC-Payroll.git
```

