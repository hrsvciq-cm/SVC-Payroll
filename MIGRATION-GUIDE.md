# دليل التحويل الكامل - من Local إلى Online

## ✅ ما تم إنجازه

### 1. البنية الأساسية
- ✅ Next.js 14 مع App Router
- ✅ Prisma ORM مع Schema مطابق تماماً
- ✅ Supabase Auth Integration
- ✅ Middleware لحماية جميع Routes
- ✅ صفحة Login احترافية

### 2. نظام المصادقة والصلاحيات
- ✅ نظام مغلق بالكامل (لا وصول بدون تسجيل دخول)
- ✅ RBAC مع 4 أدوار (Admin, HR, Finance, Viewer)
- ✅ حماية Server-side لجميع العمليات

### 3. API Routes
- ✅ `/api/employees` - إدارة الموظفين
- ✅ `/api/attendance` - تسجيل الحضور
- ✅ `/api/payroll/calculate` - حساب الرواتب
- ✅ `/api/payroll` - جلب بيانات الرواتب
- ✅ `/api/deductions` - الخصومات والمكافآت والسلف

### 4. منطق الأعمال المحفوظ
- ✅ **حساب الرواتب**: مطابق تماماً للنظام المحلي
  - استخدام 30 يوم ثابت
  - حساب العمل الإضافي والتأخيرات
  - معالجة الموظفين المنتهية خدماتهم
  - معالجة الموظفين الموقوفين
- ✅ **قواعد رؤية الموظفين**: محفوظة
  - منتهي الخدمة لا يظهر في القوائم الافتراضية
  - موقوف عن العمل يبقى ظاهراً
- ✅ **التحقق من التواريخ**: محفوظ
  - منع تسجيل دوام قبل تاريخ التعيين
  - منع تسجيل دوام بعد الإنهاء/الإيقاف

## 📋 ما يحتاج إكمال

### 1. الواجهات (UI)
- ⏳ صفحة Dashboard
- ⏳ صفحة Employees (قائمة + إضافة/تعديل)
- ⏳ صفحة Attendance (تسجيل فردي/جماعي)
- ⏳ صفحة Payroll (عرض + حساب)
- ⏳ صفحة Reports
- ⏳ استخدام shadcn/ui للتصميم

### 2. الميزات الإضافية
- ⏳ نظام النسخ الاحتياطي والاستعادة
- ⏳ الرموز السريعة (Quick Codes)
- ⏳ قسيمة الراتب (Salary Slip)
- ⏳ تصدير Excel

## 🚀 خطوات الإعداد

### 1. إعداد Supabase

```bash
# 1. أنشئ مشروع جديد على Supabase
# 2. احصل على:
#    - Project URL
#    - Anon Key  
#    - Service Role Key
#    - Database URL
```

### 2. إعداد متغيرات البيئة

أنشئ `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=your_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
DATABASE_URL=your_database_url
```

### 3. إعداد قاعدة البيانات

```bash
# توليد Prisma Client
npm run prisma:generate

# تشغيل Migrations
npm run prisma:migrate
```

### 4. إنشاء حساب Admin

1. من Supabase Dashboard > Authentication: أنشئ مستخدم
2. من Prisma Studio: أضف سجل في جدول `users`:
   ```sql
   INSERT INTO users (id, email, role, name)
   VALUES ('uuid-from-supabase', 'admin@example.com', 'admin', 'Admin');
   ```

### 5. تشغيل النظام

```bash
npm run dev
```

## 📝 ملاحظات مهمة

1. **المنطق محفوظ 100%**: جميع حسابات الرواتب والحضور مطابقة تماماً للنظام المحلي
2. **الأمان**: جميع Routes محمية، والتحقق Server-side
3. **قواعد رؤية الموظفين**: محفوظة كما هي
4. **التواريخ**: جميع التحققات من التواريخ محفوظة

## 🔄 نقل البيانات

1. من النظام المحلي: اضغط "نسخ احتياطي"
2. استخدم Prisma Studio أو script لاستيراد البيانات
3. تأكد من الحفاظ على:
   - أرقام الموظفين
   - التواريخ (hireDate, suspensionDate, terminationDate)
   - الحسابات

## 📚 الملفات المهمة

- `prisma/schema.prisma` - Schema قاعدة البيانات
- `lib/payroll-calculator.js` - منطق حساب الرواتب (مطابق للنظام المحلي)
- `middleware.js` - حماية Routes
- `lib/auth.js` - وظائف المصادقة والصلاحيات
- `app/api/` - جميع API Routes

## 🎯 الخطوات التالية

1. إنشاء الواجهات باستخدام shadcn/ui
2. إضافة نظام النسخ الاحتياطي
3. إضافة قسيمة الراتب
4. إضافة تصدير Excel
5. اختبار شامل للنظام

