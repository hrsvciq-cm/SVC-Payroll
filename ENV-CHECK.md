# التحقق من متغيرات البيئة

## المتغيرات المطلوبة

### ✅ المتغيرات الأساسية (مطلوبة)

1. **NEXT_PUBLIC_SUPABASE_URL**
   - مثال: `https://yglxbfjakoezxbrgopur.supabase.co`
   - ✅ موجود في ملفك

2. **NEXT_PUBLIC_SUPABASE_ANON_KEY** أو **NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY**
   - الكود يدعم كلا الاسمين الآن
   - ✅ موجود في ملفك باسم `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY`

3. **DATABASE_URL**
   - مثال: `postgresql://postgres.yglxbfjakoezxbrgopur:password@aws-1-eu-central-1.pooler.supabase.com:6543/postgres?pgbc`
   - ✅ موجود في ملفك

### ⚠️ المتغيرات الاختيارية (مستحسنة)

4. **SUPABASE_SERVICE_ROLE_KEY**
   - مطلوب للعمليات Server-side المتقدمة
   - يمكن الحصول عليه من: Supabase Dashboard > Settings > API > service_role key
   - ⚠️ إذا لم يكن موجوداً، بعض العمليات قد لا تعمل

5. **DIRECT_URL** (لـ Prisma Migrations)
   - ✅ موجود في ملفك - جيد جداً!
   - يستخدم للمigrations المباشرة

## ملاحظات مهمة

### ✅ ما تم إصلاحه

- الكود الآن يدعم كلا الاسمين:
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY` (الاسم الأصلي)
  - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY` (الاسم الجديد من Supabase)

### 🔧 إعداد Prisma مع DIRECT_URL

إذا كنت تستخدم `DIRECT_URL` للمigrations، تأكد من إعداد `schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
```

## التحقق من الإعداد

بعد إعادة تشغيل السيرفر، يجب أن:

1. ✅ لا تظهر رسالة خطأ Supabase
2. ✅ صفحة Login تعمل بشكل طبيعي
3. ✅ يمكنك تسجيل الدخول

## إذا استمرت المشاكل

1. تأكد من إعادة تشغيل السيرفر بعد أي تعديل على `.env.local`
2. تأكد من عدم وجود مسافات إضافية حول `=`
3. تأكد من أن جميع القيم بين علامات اقتباس (إذا كانت تحتوي على رموز خاصة)

