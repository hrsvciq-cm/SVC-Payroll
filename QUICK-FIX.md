# إصلاح سريع - متغيرات البيئة

## ✅ ما تم إصلاحه

تم تحديث الكود ليدعم كلا الاسمين للمتغير:
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` (الاسم الأصلي)
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY` (الاسم الجديد من Supabase)

## 📋 التحقق من المتغيرات

### المتغيرات الموجودة في ملفك ✅

1. ✅ `NEXT_PUBLIC_SUPABASE_URL` - موجود
2. ✅ `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY` - موجود (الكود يدعمه الآن)
3. ✅ `DATABASE_URL` - موجود
4. ✅ `DIRECT_URL` - موجود (تم إضافته لـ Prisma Schema)

### المتغيرات الاختيارية

5. ⚠️ `SUPABASE_SERVICE_ROLE_KEY` - مستحسن إضافته
   - من Supabase Dashboard > Settings > API > service_role key
   - مطلوب للعمليات Server-side المتقدمة

## 🚀 الخطوات التالية

### 1. إعادة تشغيل السيرفر

```bash
# أوقف السيرفر (Ctrl+C) ثم:
npm run dev
```

### 2. التحقق من Prisma

```bash
# توليد Prisma Client
npm run prisma:generate

# تشغيل Migrations (إذا لم تكن قد قمت بذلك)
npm run prisma:migrate
```

### 3. إنشاء حساب Admin

1. من Supabase Dashboard > Authentication: أنشئ مستخدم
2. من Prisma Studio (`npm run prisma:studio`): أضف سجل في جدول `users`

## ✅ النتيجة المتوقعة

بعد إعادة التشغيل:
- ✅ لا تظهر رسالة خطأ Supabase
- ✅ صفحة Login تعمل بشكل طبيعي
- ✅ يمكنك تسجيل الدخول

## 🔍 إذا استمرت المشاكل

1. تأكد من إعادة تشغيل السيرفر بعد أي تعديل
2. تأكد من عدم وجود مسافات إضافية في `.env.local`
3. تأكد من أن جميع القيم بين علامات اقتباس (إذا كانت تحتوي على رموز خاصة)

