# 🔴 حل عاجل: مشكلة Prisma Client

## المشكلة
```
Unknown argument `absentType`. Available options are marked with ?.
```

هذا يعني أن **Prisma Client لم يتم تحديثه** بعد إضافة حقل `absentType` إلى قاعدة البيانات.

## ✅ الحل (يجب تنفيذه الآن)

### الخطوة 1: إيقاف خادم التطوير

**في Terminal الذي يشغل `npm run dev`**:
- اضغط `Ctrl + C` لإيقاف الخادم
- تأكد من إيقاف الخادم بالكامل

### الخطوة 2: إيقاف جميع عمليات Node.js

افتح PowerShell جديد (ليس Terminal الذي يستخدمه الخادم) واكتب:

```powershell
Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force
```

أو استخدم Task Manager:
1. اضغط `Ctrl + Shift + Esc`
2. ابحث عن `node.exe`
3. انقر بزر الماوس الأيمن → End Task

### الخطوة 3: تشغيل السكريبت

في PowerShell (كمسؤول إذا أمكن):

```powershell
cd "C:\Users\HR\Videos\Payroll System"
.\fix-prisma-client.ps1
```

أو يدوياً:

```powershell
cd "C:\Users\HR\Videos\Payroll System"
npx prisma generate
```

### الخطوة 4: إذا فشل الأمر

إذا استمر الخطأ `EPERM`:

1. **أغلق Cursor/VS Code بالكامل**
2. **أعد تشغيل الكمبيوتر**
3. بعد إعادة التشغيل:
   ```powershell
   cd "C:\Users\HR\Videos\Payroll System"
   npx prisma generate
   ```

### الخطوة 5: إعادة تشغيل الخادم

بعد نجاح `prisma generate`:

```bash
npm run dev
```

## ✅ التحقق من الحل

بعد إعادة تشغيل الخادم، جرب تسجيل دوام موظف. يجب أن يعمل بدون أخطاء.

## 📝 ملاحظة

- قاعدة البيانات **تم تحديثها بنجاح** (الحقل موجود)
- المشكلة فقط في **Prisma Client** المحلي
- بعد إعادة توليد Prisma Client، سيعمل كل شيء بشكل صحيح

